# Core dependencies
requests>=2.31.0
qdrant-client>=1.7.0
chonkie>=0.1.0
semchunk>=2.0.0
tiktoken>=0.5.0
tqdm>=4.66.0

# Ragify-specific dependencies
tika>=3.0.0  # Apache Tika for universal file extraction
pydantic>=2.0.0  # Configuration validation
pyyaml>=6.0  # YAML config file support
structlog>=23.0.0  # Structured logging

# Optional (for URL processing compatibility)
beautifulsoup4>=4.12.0  # HTML parsing for web content
